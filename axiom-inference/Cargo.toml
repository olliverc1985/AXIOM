[package]
name = "axiom-inference"
version = "0.1.0"
edition = "2021"
description = "AXIOM inference — route sentences through a trained AXIOM model"

[dependencies]
axiom-core = { path = "../axiom-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
